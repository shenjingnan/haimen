name: Copy Installers for User-Friendly URLs

on:
  release:
    types: [published]

jobs:
  copy-installers:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: Copy install.sh -> install.sh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          echo "Downloading installers from release ${TAG}..."
          gh release download "$TAG" --pattern "install.sh"
          gh release download "$TAG" --pattern "install.ps1"
          cp install.sh install.sh
          cp install.ps1 install.ps1
          gh release upload "$TAG" install.sh install.ps1 --clobber
